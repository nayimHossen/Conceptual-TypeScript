type heroIncomeType = number | string;

const heroIncome: heroIncomeType = 23000;

type HeroType = {
  name: string;
  age: number;
};

const hero4: HeroType = {
  name: "Nayim",
  age: 32,
};

const hero5: HeroType = {
  name: "Hossen",
  age: 20,
};
